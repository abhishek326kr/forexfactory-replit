I'll create a comprehensive, structured prompt that you can give to your Replit Agent to implement all the SEO improvements. Here's the perfect prompt:

***

# 🎯 **AGENT PROMPT: Implement Comprehensive SEO System for ForexFactory Blog Platform**

## **PROJECT OVERVIEW**
ForexFactory.cc is a Forex trading blog platform built with React + Express + PostgreSQL. The site publishes blog posts about Forex strategies, Expert Advisors (EAs), and MetaTrader indicators. The primary goal is to achieve **maximum SEO performance** and rank #1 on Google for target keywords.

***

## **YOUR MISSION**
Implement a complete, automated SEO infrastructure that ensures every piece of content is instantly indexed by search engines, properly structured for rich snippets, and optimized for maximum organic visibility.

***

## **CORE REQUIREMENTS**

### **1. AUTOMATIC SITEMAP GENERATION SYSTEM**

**Create: `server/seo/sitemap-generator.ts`**

Implement a dynamic sitemap system that:

- **Auto-regenerates sitemap** whenever:
  - A blog post is published, updated, or deleted
  - A category is created, updated, or deleted  
  - A signal is published or deleted
  - Any page content changes

- **Generate multiple sitemaps** for better organization:
  ```
  /sitemap.xml           -> Main sitemap index
  /sitemap-posts.xml     -> All blog posts (paginated if >50k URLs)
  /sitemap-categories.xml -> All categories
  /sitemap-signals.xml   -> All trading signals
  /sitemap-pages.xml     -> Static pages (about, contact, etc.)
  /sitemap-images.xml    -> All images with metadata
  ```

- **Include proper XML formatting:**
  ```xml
  <url>
    <loc>https://forexfactory.cc/post-slug</loc>
    <lastmod>2025-11-06</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
    <image:image>
      <image:loc>image-url</image:loc>
      <image:title>Image title</image:title>
      <image:caption>Image caption</image:caption>
    </image:image>
  </url>
  ```

- **Performance features:**
  - Cache generated sitemaps for 1 hour
  - Gzip compression for faster delivery
  - Async generation (don't block requests)
  - Support for 50,000+ URLs with pagination

**API Endpoints:**
```typescript
GET /sitemap.xml           // Main sitemap index
GET /sitemap-posts.xml     // Posts sitemap
GET /sitemap-categories.xml // Categories sitemap
GET /sitemap-signals.xml   // Signals sitemap
GET /sitemap-images.xml    // Images sitemap
POST /api/admin/seo/regenerate-sitemap // Manual regeneration
```

***

### **2. INSTANT INDEXING WITH INDEXNOW PROTOCOL**

**Create: `server/seo/indexing-service.ts`**

Implement real-time search engine notifications using IndexNow protocol:

- **Auto-notify search engines** when:
  - New blog post is published
  - Existing post is updated
  - Post is deleted (return 404/410)
  - Category or page changes

- **Support these search engines:**
  - Google
  - Bing
  - Yandex
  - Seznam.cz
  - Naver

- **Implementation requirements:**
  ```typescript
  interface IndexingService {
    // Submit single URL for indexing
    submitUrl(url: string, action: 'created' | 'updated' | 'deleted'): Promise<void>;
    
    // Submit multiple URLs in batch
    submitBatch(urls: string[]): Promise<void>;
    
    // Retry failed submissions
    retryFailedSubmissions(): Promise<void>;
  }
  ```

- **IndexNow endpoint:**
  ```
  POST https://api.indexnow.org/indexnow
  {
    "host": "forexfactory.cc",
    "key": "your-indexnow-key",
    "keyLocation": "https://forexfactory.cc/indexnow-key.txt",
    "urlList": ["url1", "url2", "url3"]
  }
  ```

- **Features:**
  - Generate unique IndexNow API key
  - Store key in `/indexnow-key.txt` endpoint
  - Log all submissions to database
  - Retry failed submissions after 1 hour
  - Rate limiting (max 10,000 URLs/day)
  - Show submission status in admin dashboard

**Trigger indexing automatically in:**
- `POST /api/admin/posts` (after creating post)
- `PUT /api/admin/posts/:id` (after updating post)
- `DELETE /api/admin/posts/:id` (after deleting post)
- `POST /api/admin/signals` (after creating signal)

***

### **3. GOOGLE SEARCH CONSOLE INTEGRATION**

**Create: `server/seo/google-search-console.ts`**

Integrate with Google Search Console API for:

- **URL Inspection & Indexing API:**
  ```typescript
  // Request indexing for a URL
  async function requestGoogleIndexing(url: string): Promise<void> {
    // Use Google Indexing API
    // POST https://indexing.googleapis.com/v3/urlNotifications:publish
  }
  
  // Check indexing status
  async function checkIndexingStatus(url: string): Promise<IndexStatus> {
    // Use URL Inspection API
    // Returns: indexed, not_indexed, crawled_not_indexed, etc.
  }
  ```

- **Setup requirements:**
  - Service account JSON credentials in `.env`
  - Verify domain ownership in Search Console
  - Grant indexing API permissions
  - Store credentials securely

- **Features:**
  - Auto-request indexing on publish
  - Check indexing status daily
  - Show in admin dashboard
  - Alert if pages not indexed after 7 days

**Environment variables needed:**
```env
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_PROJECT_ID=your-project-id
```

***

### **4. STRUCTURED DATA (JSON-LD) IMPLEMENTATION**

**Create: `server/seo/structured-data.ts`**

Generate Schema.org JSON-LD for every page type:

#### **A. Blog Post Schema (BlogPosting)**
```typescript
function generateBlogPostSchema(post: Post): string {
  return JSON.stringify({
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": post.title,
    "image": [post.featuredImage],
    "datePublished": post.publishedAt,
    "dateModified": post.updatedAt,
    "author": {
      "@type": "Person",
      "name": post.author,
      "url": `https://forexfactory.cc/author/${post.authorSlug}`
    },
    "publisher": {
      "@type": "Organization",
      "name": "ForexFactory",
      "logo": {
        "@type": "ImageObject",
        "url": "https://forexfactory.cc/logo.png"
      }
    },
    "description": post.metaDescription,
    "mainEntityOfPage": {
      "@type": "WebPage",
      "@id": post.url
    },
    "keywords": post.tags.join(", "),
    "articleSection": post.category,
    "wordCount": post.wordCount,
    "inLanguage": "en-US"
  });
}
```

#### **B. Breadcrumb Schema**
```typescript
function generateBreadcrumbSchema(items: BreadcrumbItem[]): string {
  return JSON.stringify({
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": items.map((item, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "name": item.name,
      "item": item.url
    }))
  });
}
```

#### **C. Organization Schema (for homepage)**
```typescript
const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "ForexFactory",
  "url": "https://forexfactory.cc",
  "logo": "https://forexfactory.cc/logo.png",
  "description": "Professional Forex trading resources, Expert Advisors, and indicators for MetaTrader",
  "sameAs": [
    "https://twitter.com/forexfactory",
    "https://facebook.com/forexfactory",
    "https://youtube.com/@forexfactory"
  ],
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "customer service",
    "email": "support@forexfactory.cc"
  }
};
```

#### **D. FAQ Schema (if post contains FAQs)**
```typescript
function generateFAQSchema(faqs: {question: string, answer: string}[]): string {
  return JSON.stringify({
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": faqs.map(faq => ({
      "@type": "Question",
      "name": faq.question,
      "acceptedAnswer": {
        "@type": "Answer",
        "text": faq.answer
      }
    }))
  });
}
```

#### **E. SoftwareApplication Schema (for EA/indicator downloads)**
```typescript
function generateSoftwareSchema(software: Software): string {
  return JSON.stringify({
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": software.name,
    "operatingSystem": "Windows",
    "applicationCategory": "FinanceApplication",
    "offers": {
      "@type": "Offer",
      "price": "0",
      "priceCurrency": "USD"
    },
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": software.rating,
      "reviewCount": software.reviews
    }
  });
}
```

**Integration:**
- Add schema to `<head>` section of every page
- Validate using Google's Rich Results Test
- Test in admin dashboard before publishing

***

### **5. COMPLETE META TAGS & OPEN GRAPH SYSTEM**

**Create: `server/seo/meta-tags.ts`**

Generate comprehensive meta tags for every page:

```typescript
interface MetaTags {
  // Basic SEO
  title: string;               // Max 60 chars
  description: string;         // Max 160 chars
  keywords: string[];
  canonical: string;
  robots: string;              // "index, follow"
  
  // Open Graph (Facebook/LinkedIn)
  ogType: string;              // "article"
  ogTitle: string;
  ogDescription: string;
  ogImage: string;             // 1200x630px
  ogImageWidth: string;
  ogImageHeight: string;
  ogUrl: string;
  ogSiteName: string;
  articlePublishedTime: string;
  articleModifiedTime: string;
  articleAuthor: string;
  articleSection: string;
  articleTag: string[];
  
  // Twitter Card
  twitterCard: string;         // "summary_large_image"
  twitterSite: string;         // "@forexfactory"
  twitterTitle: string;
  twitterDescription: string;
  twitterImage: string;
  
  // Additional
  author: string;
  publisher: string;
  copyright: string;
  language: string;
}
```

**HTML output example:**
```html
<title>Top 5 Forex Scalping Strategies 2025 | ForexFactory</title>
<meta name="description" content="Learn the best forex scalping strategies used by professional traders. Step-by-step guide with MT4/MT5 indicators and free EA downloads." />
<meta name="keywords" content="forex scalping, scalping strategy, metatrader, forex trading, day trading" />
<link rel="canonical" href="https://forexfactory.cc/forex-scalping-strategies-2025" />
<meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large" />

<!-- Open Graph -->
<meta property="og:type" content="article" />
<meta property="og:title" content="Top 5 Forex Scalping Strategies 2025" />
<meta property="og:description" content="Learn the best forex scalping strategies..." />
<meta property="og:image" content="https://forexfactory.cc/images/scalping-strategies.jpg" />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:url" content="https://forexfactory.cc/forex-scalping-strategies-2025" />
<meta property="og:site_name" content="ForexFactory" />
<meta property="article:published_time" content="2025-11-06T10:00:00+00:00" />
<meta property="article:author" content="John Doe" />
<meta property="article:section" content="Trading Strategies" />
<meta property="article:tag" content="forex, scalping, metatrader" />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:site" content="@forexfactory" />
<meta name="twitter:title" content="Top 5 Forex Scalping Strategies 2025" />
<meta name="twitter:description" content="Learn the best forex scalping strategies..." />
<meta name="twitter:image" content="https://forexfactory.cc/images/scalping-strategies.jpg" />
```

**Features:**
- Auto-generate from post data
- Allow manual override in admin editor
- Truncate to proper lengths
- Escape special characters
- Preview in admin before publishing

***

### **6. RSS/ATOM FEED GENERATION**

**Create: `server/seo/rss-feed.ts`**

Generate RSS 2.0 and Atom feeds:

**Main RSS feed (`/rss.xml` or `/feed.xml`):**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>ForexFactory - Forex Trading Strategies</title>
    <link>https://forexfactory.cc</link>
    <description>Latest forex trading strategies, EAs, and indicators</description>
    <language>en-us</language>
    <lastBuildDate>Wed, 06 Nov 2025 14:00:00 +0000</lastBuildDate>
    <atom:link href="https://forexfactory.cc/rss.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Post Title</title>
      <link>https://forexfactory.cc/post-slug</link>
      <description>Post excerpt or full content</description>
      <content:encoded><![CDATA[Full HTML content here]]></content:encoded>
      <pubDate>Wed, 06 Nov 2025 10:00:00 +0000</pubDate>
      <guid isPermaLink="true">https://forexfactory.cc/post-slug</guid>
      <category>Trading Strategies</category>
      <author>author@forexfactory.cc (Author Name)</author>
      <enclosure url="https://forexfactory.cc/images/featured.jpg" type